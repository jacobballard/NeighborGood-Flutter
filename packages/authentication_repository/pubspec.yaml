name: authentication_repository
description: Dart package which manages the authentication domain.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=2.19.4 <3.0.0"

dependencies:
  cache:
    path: ../cache
  equatable:
  firebase_auth:
  firebase_core:

  flutter:
    sdk: flutter
  google_sign_in:
  meta:
